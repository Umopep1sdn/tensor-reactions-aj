-- Minimal valid 'test' timeline profile (runs at the SSS dummy in Kholusia).
-- Placeholder so both GeneralReactions/ and TimelineReactions/ exist with real
-- .lua content (mirrors working third-party repos). Real DMU phase timelines
-- will be added here later, anchored to the live DMU timeline (P4 sync = [800]).
local tbl =
{
	inheritedProfiles =
	{
	},
	timelineName = "test",
	version = "1.0.0",
}

return tbl
